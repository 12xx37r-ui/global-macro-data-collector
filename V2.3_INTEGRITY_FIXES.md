# Cards 8–12 V2.3 integrity fixes

- CME references and collection routes remain excluded.
- Card 10 is now published as a true pressure index: higher means more inflationary/supply pressure; lower means easing.
- GSCPI collection validates HTTP payloads before opening them as Excel and uses official workbook → FRED CSV fallback → last successful local cache.
- Card 9/10 quality scores now distinguish successful collection from freshness. Stale series are listed explicitly.
- Card 12 replaces the broken Yahoo `DX=F` route with `DX-Y.NYB` as a clearly labeled dollar-index proxy, not a futures quote.
- Card 11 now uses validated horizons and data completeness together; `passed` and `level` can no longer contradict each other.
- Card 11 includes Korean input labels and validation details for dashboard rendering.
