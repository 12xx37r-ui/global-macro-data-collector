# V140 three core assets

Added OOS validation keys: `oil` (CL=F), `emerging` (EEM), and `investmentGrade` (LQD).
Expected output: `public/data/asset_oos_validation.json` with 13 assets and zero errors when all sources succeed.
