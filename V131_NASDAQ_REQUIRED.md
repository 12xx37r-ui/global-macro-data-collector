# V131 Nasdaq required OOS

- `nasdaq` is a required asset in `asset_oos_validation.json`.
- Price target: QQQ.
- Card 12 source group: NQ=F with ES=F fallback.
- Workflow verifies JSON creation and commits it to `public/data`.
